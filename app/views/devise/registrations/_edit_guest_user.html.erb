<h3 class="bg-red-100 border border-red-400 text-red-700 my-4 px-4 py-3 rounded text-center">ゲストユーザーは変更・削除ができません</h3>

<%= render 'shared/user_image', user: resource %>

<div class="field my-4">
  <%= f.label :name, class: "block font-light mb-2" %>
  <%= f.text_field :name, disabled: true, readonly: true, class: "block w-full px-3 py-2 border border-gray-300 bg-gray-200 shadow-sm rounded" %>
</div>

<div class="field my-4">
  <%= f.label :email, class: "block font-light mb-2" %>
  <%= f.email_field :email, disabled: true, readonly: true, class: "block w-full px-3 py-2 border bg-gray-200 border-gray-300 shadow-sm rounded" %>
</div>

<% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
<div>
  Currently waiting confirmation for: <%= resource.unconfirmed_email %>
</div>
<% end %>

<div class="field my-4">
  <%= f.label :image, class: "block font-light mb-2" %>
  <%= f.file_field :image, disabled: true %>
</div>

<div class="actions text-center">
  <%= f.submit "更新", disabled: true, class: "btn cursor-not-allowed" %>
</div>

<p class="mt-6 block">
  <%= button_to "アカウント削除", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete,disabled: true, class: "font-light underline hover:text-black cursor-not-allowed" %>
</p>
