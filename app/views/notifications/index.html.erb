<% provide(:title, '通知') %>

<div>
  <h1 class="text-center text-xl mb-4">通知</h1>
  <% if @notifications.present? %>
    <%= render @notifications %>
  <% else %>
    通知はありません
  <% end %>

  <%= paginate @notifications %>
</div>
